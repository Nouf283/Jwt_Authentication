@if (User.Identity.IsAuthenticated)
{
    <form method="post" asp-page="/Logout">
        Welcome @User.Identity.Name
        <button type="submit" class="ml-2 btn btn-link">Logout</button>
    </form>
}
else
{
    <a class="btn btn-link" asp-page="/Login/_Login">Login</a>
}
